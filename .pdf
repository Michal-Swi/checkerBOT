#include <bits/stdc++.h>

using namespace std;

int main(int argc, char **argv) {
	if (argc < 2) {
        cout << "No file. FATAL";
        return 1;
    } else {
        cout << argv[1] << endl;
    }

    ifstream file(argv[1]);

	while (!file.eof()) {
		
	}	

	return 0;
}